# 集合: 会自动去重的列表
## 1 基本语法
```python
<set name> = {a0, a1, a2,...}
```

## 2 基本操作
```python
s = {1, 2, 3, 4}
t = {3, 4, 5, 6}

# 并集
s | t          # {1, 2, 3, 4, 5, 6}

# 交集
s & t          # {3, 4}

# 差集
s - t          # {1, 2}

# 对称差/异或
s ^ t          # {1, 2, 5, 6}
```

## 3 常用方法
```python
s.add(5)        # 增
# 删
s.remove(3)     # 不存在报错
s.discard(3)    # 不存在不报错
s.pop()         # 随机返回一个元素并删除它 (改变原集合) 
s.clear()       # 清空
```

## 4 高级用法
```python
# 去重
unique = set([1, 1, 2, 2, 3])   # {1, 2, 3}

# 成员检查
if x in s:
    print("存在")

s.issubset(t)   # 子集 (s的所有元素在t中都能找到) 
s.issuperset(t) # 超集 (与子集相反) 
```

## 5 注意事项
1. Python的集合与数学上的集合基本一致, 都是自动去重, 并且是无序的
2. 空集合是`set()`不是`{}`
